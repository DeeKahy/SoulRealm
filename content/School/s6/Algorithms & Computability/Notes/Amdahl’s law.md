

#### Amdahl’s law
$$ S(n) = \frac{1}{(1 - p) + \frac{p}{n}} $$

Where:  
- $S(n)$: Theoretical speedup with $ n $ processors  
- $p$: Parallelizable fraction of the workload ( $0 \leq p \leq 1$)  
- $n$: Number of processors ($n \geq 1$)

$$\frac{1}{(1 - 0.4) + \frac{0.4}{2}} $$

